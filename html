<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Para Sempre Nós 💖</title>
  <style>
    body {
      margin: 0;
      font-family: 'Segoe UI', sans-serif;
      background: linear-gradient(to bottom, #ffe4ec, #fff0f5);
      overflow-x: hidden;
      overflow-y: hidden;
      position: relative;
      color: #d6336c;
      text-align: center;
      height: 100vh;
    }

    h1 {
      font-size: 2.5em;
      margin-top: 30px;
      animation: fadeInDown 2s ease;
      z-index: 1;
      position: relative;
    }

    @keyframes fadeInDown {
      from { opacity: 0; transform: translateY(-20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .carousel {
      display: flex;
      overflow-x: auto;
      scroll-snap-type: x mandatory;
      padding: 20px;
      gap: 20px;
      margin-top: 20px;
      position: relative;
      z-index: 1;
    }

    .carousel img {
      height: 300px;
      border-radius: 20px;
      scroll-snap-align: center;
      flex: 0 0 auto;
      box-shadow: 0 5px 15px rgba(0,0,0,0.2);
      transition: transform 0.3s;
    }

    .carousel img:hover {
      transform: scale(1.05);
    }

    .mensagem {
      font-size: 20px;
      padding: 20px;
      max-width: 90%;
      margin: 0 auto;
      animation: fadeIn 3s ease;
      z-index: 1;
      position: relative;
    }

    @keyframes fadeIn {
      0% { opacity: 0; transform: translateY(10px); }
      100% { opacity: 1; transform: translateY(0); }
    }

    .audio {
      margin: 30px auto;
      z-index: 1;
      position: relative;
    }

    .digitar {
      font-family: monospace;
      white-space: nowrap;
      overflow: hidden;
      border-right: 3px solid pink;
      animation: typing 4s steps(40, end), blink 0.75s step-end infinite;
      max-width: 90%;
      margin: 0 auto;
      font-size: 22px;
    }

    @keyframes typing {
      from { width: 0 }
      to { width: 100% }
    }

    @keyframes blink {
      from, to { border-color: transparent }
      50% { border-color: pink; }
    }

    /* Corações flutuando */
    .hearts {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      pointer-events: none;
      z-index: 0;
    }

    .heart {
      position: absolute;
      width: 20px;
      height: 20px;
      background: pink;
      transform: rotate(45deg);
      animation: float 10s linear infinite;
      opacity: 0.6;
      bottom: -20px;
    }

    .heart::before,
    .heart::after {
      content: "";
      position: absolute;
      width: 20px;
      height: 20px;
      background: pink;
      border-radius: 50%;
    }

    .heart::before {
      top: -10px;
      left: 0;
    }

    .heart::after {
      left: -10px;
      top: 0;
    }

    @keyframes float {
      0% {
        transform: translateY(0) rotate(45deg);
        opacity: 0.6;
      }
      100% {
        transform: translateY(-100vh) rotate(45deg);
        opacity: 0;
      }
    }

    .audio audio {
      border-radius: 10px;
      box-shadow: 0 0 10px rgba(255, 192, 203, 0.4);
    }
  </style>
</head>
<body>
  <!-- Corações flutuantes -->
  <div class="hearts">
    <div class="heart" style="left: 10%; animation-delay: 0s;"></div>
    <div class="heart" style="left: 30%; animation-delay: 2s;"></div>
    <div class="heart" style="left: 50%; animation-delay: 4s;"></div>
    <div class="heart" style="left: 70%; animation-delay: 1s;"></div>
    <div class="heart" style="left: 90%; animation-delay: 3s;"></div>
  </div>

  <h1>❤ Amanda & João ❤</h1>

  <div class="carousel">
    <img src="https://via.placeholder.com/250x300?text=Foto+1" alt="Foto 1" />
    <img src="https://via.placeholder.com/250x300?text=Foto+2" alt="Foto 2" />
    <img src="https://via.placeholder.com/250x300?text=Foto+3" alt="Foto 3" />
  </div>

  <div class="mensagem digitar">
    “E de todos os caminhos que já trilhei, o mais bonito foi aquele que me levou até você.”
  </div>

  <div class="audio">
    <p>Clique abaixo para ouvir nossa música 🎶</p>
    <audio controls>
      <source src="https://www.soundhelix.com/examples/mp3/SoundHelix-Song-1.mp3" type="audio/mpeg" />
      Seu navegador não suporta o áudio.
    </audio>
  </div>
</body>
</html>
